@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@

@section Sidebar{
    <a asp-area="" asp-controller="Isakymai" asp-action="Orders">Įsakymai</a>
    <a class="active" asp-area="" asp-controller="Isakymai" asp-action="Contracts">Sutartys</a>
    <a asp-area="" asp-controller="Isakymai" asp-action="Payments">Įmokos</a>
}


@section Modals{
    <div class="modal fade" id="newEntryModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">

                <div class="modal-header">
                    <h5 class="modal-title" id="textModalLabel">Pervadinti</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>

                <div class="modal-body">
                    <!-- CIA VISI LAUKAI KURIE RODOMI PRIDEJIMO IR REDAGAVIMO METU, REIKES PAKEISTI requestInput FUNKCIJA -->
                    <div class="input-group mb-3">
                        <input id="inputModalName" type="text" class="form-control" placeholder="Numeris" aria-label="Įsakymo numeris" aria-describedby="basic-addon1">
                    </div>

                    <div class="input-group mb-3">
                        <textarea id="inputModalText" type="text" class="form-control" placeholder="Pavadinimas" aria-label="Įsakymo pavadinimas" aria-describedby="basic-addon1"></textarea>
                    </div>

                    <div class="input-group mb-3">
                        <input id="inputModalTime" type="date" class="form-control" placeholder="Data" aria-label="Priskyrimo data" aria-describedby="basic-addon1">
                    </div>

                    <div class="input-group mb-3">
                        <input id="inputModalTimeUntil" type="date" class="form-control" placeholder="Iki" aria-label="Galioja iki" aria-describedby="basic-addon1">
                    </div>
                </div>

                <div class="modal-footer">
                    <button id="inputModalConfirm" type="button" class="btn btn-success" data-dismiss="modal">Redaguoti</button>
                    <button type="button" class="btn btn-primary" data-dismiss="modal">Atšaukti</button>
                </div>
            </div>
        </div>
    </div>

    <div class="modal fade" id="viewEntryModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
        <div class="modal-dialog" role="document">
            <div class="modal-content">

                <div class="modal-header">
                    <h5 class="modal-title" id="viewModalLabel">Peržiūra</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>

                <div class="modal-body">
                    <!-- CIA VISI LAUKAI TOKIE PATYS KAIP PRAITAM TIK VISI TURI disabled -->
                    <div class="input-group mb-3">
                        <input disabled id="viewModalName" type="text" class="form-control" placeholder="Numeris" aria-label="Įsakymo numeris" aria-describedby="basic-addon1">
                    </div>

                    <div class="input-group mb-3">
                        <textarea disabled id="viewModalText" type="text" class="form-control" placeholder="Pavadinimas" aria-label="Įsakymo pavadinimas" aria-describedby="basic-addon1"></textarea>
                    </div>

                    <div class="input-group mb-3">
                        <input disabled id="viewModalTime" type="date" class="form-control" placeholder="Data" aria-label="Priskyrimo data" aria-describedby="basic-addon1">
                    </div>

                    <div class="input-group mb-3">
                        <input disabled id="viewModalTimeUntil" type="date" class="form-control" placeholder="Iki" aria-label="Galioja iki" aria-describedby="basic-addon1">
                    </div>
                </div>

                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-dismiss="modal">Uždaryti</button>
                </div>
            </div>
        </div>
    </div>
}


<table class="table" style="width: 100%">
    @{
        List<SelectListItem> listItems = new List<SelectListItem>();
        listItems.Add(new SelectListItem
        {
            Text = "Einamojo semestro sutartys",
            Value = "Einamojo semestro sutatys",
            Selected = true

        });
        listItems.Add(new SelectListItem
        {
            Text = "Visos sutartys",
            Value = "Visos sutartys"
        });
    }

    <div class="isp" style="margin-bottom: 15px;">
        <i class="fas fa" style="color: #3c5c4a;"></i>
        <span class="name">
            @Html.DropDownList("yourDropName", listItems, new { @class = "form-control" })
        </span>
    </div>

    @if (ViewBag.Admin)
    {
        <tr>
            <th>Visos sutartys</th>
        </tr>
        <tr>
            <td>
                <div class="isp-item-list col">

                    <div class="isp-item" id="CreateContract" style="margin-bottom: 15px;">
                        <i class="fas fa-plus" style="color: #3c5c4a;"></i>
                        <span class="name">Nauja sutartis</span>
                    </div>

                    @for (int i = 0; i < 10; i++)
                    {
                        string tooltipDirection = i == 0 ? "bottom" : "top";

                        <div class="isp-item" name="@i">
                            <span class="name">Sutartis @i</span>
                            <span class="right">'Sutartis galioja iki: 2024-06-30'</span>


                            <div class="vertical-line"></div>

                            <div class="icons icons-count-2">
                                <i class="fas fa-pen" data-toggle="tooltip" data-placement="@tooltipDirection" title="Redaguoti"></i>

                                <i class="fas"></i>

                                <i class="fas fa-trash" data-toggle="tooltip" data-placement="@tooltipDirection" title="Ištrinti"></i>
                            </div>

                        </div>
                    }
                </div>
            </td>
        </tr>
    }

    <tr>
        <th>Mano sutartys</th>
    </tr>

    <tr>
        <td>
            <div class="isp-item-list col">
                @for (int i = 0; i < 10; i++)
                {
                    string tooltipDirection = i == 0 ? "bottom" : "top";

                    <div class="isp-item" name="@i">
                        <span class="name">Sutartis @i</span>
                        <span class="right-no-gap">'Sutartis galioja iki: 2024-06-30'</span>
                    </div>
                }
            </div>
        </td>
    </tr>
</table>

@section Scripts {
    <script>
        function deleteConfirmed() {
            // Funkcija iskviecia kai patvirtintas istrynimas, reikia ajax requesto
        }

        function getEntryName(element) {
            return $(element).closest(".isp-item").attr("name");
        }

        function deleteEntry(element) {
            $("#modalEntryName").text(getEntryName(element));
            $('#deleteEntryModal').modal();
        }

        function editEntry(element) {
            requestInputAsng(
                "Redaguoti",
                getEntryName(element),
                "SUTARTIES TEKSTAS",
                "2020-09-01",
                "2024-06-30",
                "Redaguoti",
                function () {
                    // Ajax
                });
        }

        function requestInputAsng(header, initialText1, initialText2, initialText3, initialText4, btnText, clickcb) {
            $("#textModalLabel").text(header);
            $("#inputModalConfirm").text(btnText);
            $("#inputModalConfirm").off("click");
            $("#inputModalConfirm").click(clickcb);
            $("#inputModalName").val(initialText1);
            $("#inputModalText").val(initialText2);
            $("#inputModalTime").val(initialText3);
            $("#inputModalTimeUntil").val(initialText4);
            $('#newEntryModal').modal();
        }

        function newEntry() {
            requestInputAsng(
                "Nauja sutartis",
                "",
                "",
                "",
                "",
                "Sukurti",
                function () {
                    // Cia normalu ajax reikes ideti, cia tik pavyzdinis
                    @*
                    let name = $("#id").val();

                    $.ajax({
                        url: '@Url.Action(Action, Controller)',
                        type: 'POST',
                        data: {
                            details: {
                                vardas: reiksme
                            }
                        },
                        success: function (data) {
                            if (data === 'True') {
                                // Kazkas kai pavyko
                            }
                            else {
                                // Kazkas blogai, arba jau egzistuoja arba dar kazkas
                            }
                        },
                        error: function () {
                            // Nepavyko ajax issiusti
                        }
                    });*@
                });
        }

        function selectElement(element) {
            $("#viewModalName").val(getEntryName(element));
            $("#viewModalText").val("SUTARTIES TEKSTAS");
            $("#viewModalTime").val("2020-09-01");
            $("#viewModalTimeUntil").val("2024-06-30");
            $('#viewEntryModal').modal();
        }

        $(document).ready(function () {
            // Don't ask
            $(function () {
                $('[data-toggle="tooltip"]').tooltip()
            });

            // Ištrinti
            $(".isp-item").on('click', '.fa-trash', function (event) {
                event.stopPropagation();
                deleteEntry($(this));
            });

            // Redaguoti
            $(".isp-item").on('click', '.fa-pen', function (event) {
                event.stopPropagation();
                editEntry($(this));
            });

            // Sukurimas
            $("#CreateContract").click(function (event) {
                event.stopPropagation();
                newEntry();
            });

            // Paspaudimas ant elemento
            $(".isp-item-list").on("click", ".isp-item", function (event) {
                event.stopPropagation();
                selectElement($(this));
            });
        })
    </script>
}
